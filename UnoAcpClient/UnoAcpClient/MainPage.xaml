<Page
    x:Class="UnoAcpClient.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UnoAcpClient"
    xmlns:vm="using:UnoAcpClient.Presentation.ViewModels"
    xmlns:converters="using:UnoAcpClient.Presentation.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:ConnectionStatusToColorConverter x:Key="StatusToColorConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:MessageDirectionToAlignmentConverter x:Key="DirectionToAlignmentConverter"/>
    </Page.Resources>

    <Grid RowDefinitions="Auto,*,Auto">
        <!-- 顶部状态栏 -->
        <StackPanel Grid.Row="0" Padding="16" Orientation="Vertical" Spacing="8">
            <!-- 连接状态 -->
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Ellipse Width="12" Height="12" Fill="{Binding CurrentConnectionState.Status, Converter={StaticResource StatusToColorConverter}}"/>
                <TextBlock Text="连接状态:" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding CurrentConnectionState.Status}" VerticalAlignment="Center" FontWeight="Bold"/>
                <TextBlock Text="{Binding CurrentConnectionState.ServerUrl}" VerticalAlignment="Center" Opacity="0.7"/>
            </StackPanel>

            <!-- 服务器选择 -->
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="服务器:" VerticalAlignment="Center" Width="60"/>
                <ComboBox ItemsSource="{Binding Servers}"
                          SelectedItem="{Binding SelectedServer, Mode=TwoWay}"
                          DisplayMemberPath="Name"
                          Width="200"/>
                <Button Content="连接" Command="{Binding ConnectCommand}"/>
                <Button Content="断开" Command="{Binding DisconnectCommand}"/>
            </StackPanel>

            <!-- 错误信息 -->
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Visibility="{Binding ErrorMessage, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}"/>
        </StackPanel>

        <!-- 消息历史 -->
        <ScrollViewer Grid.Row="1" Padding="16">
            <ItemsControl ItemsSource="{Binding MessageHistory}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="4" HorizontalAlignment="{Binding IsOutgoing, Converter={StaticResource DirectionToAlignmentConverter}}">
                            <Border Background="{ThemeResource CardBackgroundFillColorDefault}"
                                    BorderBrush="{ThemeResource CardBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Padding="8">
                                <StackPanel>
                                    <TextBlock Text="{Binding Method}" FontWeight="Bold" FontSize="12"/>
                                    <TextBlock Text="{Binding Content}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Timestamp}" FontSize="10" Opacity="0.6"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- 消息输入 -->
        <StackPanel Grid.Row="2" Padding="16" Orientation="Vertical" Spacing="8">
            <TextBox PlaceholderText="方法名 (例如：tools/list)" Text="{Binding Method, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox PlaceholderText="参数 (JSON 格式)" Text="{Binding Parameters, Mode=TwoWay}" TextWrapping="Wrap" Height="80"/>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button Content="发送" Command="{Binding SendMessageCommand}"/>
                <Button Content="清除历史" Command="{Binding ClearHistoryCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
