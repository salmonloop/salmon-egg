<Page
    x:Class="UnoAcpClient.Presentation.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UnoAcpClient.Presentation.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowDefinitions="Auto,*" Padding="16">
        <!-- 标题栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Margin="0,0,0,16">
            <TextBlock Text="服务器配置" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="添加配置" Click="AddConfiguration_Click"/>
            <Button Content="编辑" Click="EditConfiguration_Click"/>
            <Button Content="删除" Click="DeleteConfiguration_Click"/>
            <Button Content="刷新" Click="SaveConfiguration_Click"/>
        </StackPanel>

        <!-- 配置列表 -->
        <ListView Grid.Row="1"
                  ItemsSource="{Binding Configurations}"
                  SelectedItem="{Binding SelectedConfiguration, Mode=TwoWay}"
                  SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Padding="8">
                        <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                        <TextBlock Text="{Binding ServerUrl}" FontSize="12" Opacity="0.7"/>
                        <TextBlock Text="{Binding Transport}" FontSize="10" Opacity="0.5"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>