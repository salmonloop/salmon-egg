<Page
    x:Class="SalmonEgg.Presentation.Views.SettingsShellPage"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:SalmonEgg.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:settings="using:SalmonEgg.Presentation.ViewModels.Settings"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <Style TargetType="NavigationView">
            <!-- Keep the secondary nav visually compact and consistent across Skia/MSIX -->
            <Setter Property="AlwaysShowHeader" Value="False" />
            <Setter Property="IsBackButtonVisible" Value="Collapsed" />
            <Setter Property="IsSettingsVisible" Value="False" />
            <Setter Property="PaneDisplayMode" Value="Top" />
        </Style>

        <DataTemplate x:Key="SettingsSectionNavItemTemplate"
                      x:DataType="settings:SettingsShellSectionViewModel">
            <NavigationViewItem AutomationProperties.AutomationId="{x:Bind AutomationId}"
                                Content="{x:Bind Title, Mode=OneWay}"
                                Tag="{x:Bind Key, Mode=OneWay}" />
        </DataTemplate>
    </Page.Resources>

    <Grid x:Name="SettingsShellRoot"
          Padding="24,12"
          XYFocusKeyboardNavigation="Enabled">
        <VisualStateManager.VisualStateGroups>
            <!-- Short windows: densify shell chrome so section nav + content share vertical
                 budget. Comfortable restores prior marketing padding at MinWindowHeight=760. -->
            <VisualStateGroup x:Name="SettingsShellHeightStates">
                <VisualState x:Name="SettingsShellHeightCompact">
                    <VisualState.Setters>
                        <Setter Target="SettingsShellRoot.Padding" Value="24,12" />
                        <Setter Target="SettingsNavView.Margin" Value="0,0,0,8" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="SettingsShellHeightComfortable">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowHeight="760" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="SettingsShellRoot.Padding" Value="40,24" />
                        <Setter Target="SettingsNavView.Margin" Value="0,0,0,16" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <NavigationView x:Name="SettingsNavView"
                        Grid.Row="0"
                        Margin="0,0,0,8"
                        XYFocusKeyboardNavigation="Enabled"
                        AutomationProperties.AutomationId="SettingsNavView"
                        MenuItemsSource="{x:Bind ViewModel.Sections, Mode=OneWay}"
                        MenuItemTemplate="{StaticResource SettingsSectionNavItemTemplate}"
                        SelectedItem="{x:Bind ViewModel.SelectedSection, Mode=OneWay}" />

        <Frame x:Name="SettingsFrame"
               Grid.Row="1"
               Background="Transparent" />
    </Grid>
</Page>
