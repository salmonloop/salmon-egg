<views:SettingsPageBase
    x:Class="SalmonEgg.Presentation.Views.Settings.AcpConnectionSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:SalmonEgg.Controls"
    xmlns:views="using:SalmonEgg.Presentation.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:SalmonEgg.Presentation.ViewModels.Settings"
    xmlns:models="using:SalmonEgg.Domain.Models"
    xmlns:converters="using:SalmonEgg.Presentation.Converters"
    mc:Ignorable="d"
    Background="Transparent"
    AllowDrop="False">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <converters:HydrationCompletionModeLocalizationConverter x:Key="HydrationCompletionModeLocalizationConverter"/>

        <Style x:Key="AgentListItemStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="MinHeight" Value="64"/>
        </Style>
    </Page.Resources>

    <ScrollViewer Padding="0"
                  HorizontalScrollBarVisibility="Disabled">
        <controls:ResponsiveContentHost MaxContentWidth="{StaticResource SettingsContentMaxWidth}">
            <controls:ResponsiveContentHost.Child>
                <StackPanel Spacing="28">
                    <StackPanel Spacing="8">
                        <TextBlock x:Uid="Acp_PageTitle"
                                   Text="ACP Agent"
                                   Style="{StaticResource SettingsPageTitleTextStyle}"/>
                        <TextBlock x:Uid="Acp_PageSummary"
                                   Text="管理 ACP 连接配置、远端目录，以及会话加载策略。"
                                   Style="{StaticResource SettingsPageSummaryTextStyle}"/>
                    </StackPanel>

                    <StackPanel Spacing="12">
                        <TextBlock x:Uid="Acp_GlobalTitle"
                                   Text="全局 ACP 配置"
                                   Style="{StaticResource SettingsSectionTitleTextStyle}"/>

                        <Border Style="{StaticResource SettingsSectionContainerStyle}">
                            <Grid Style="{StaticResource SettingsRowGridStyle}"
                                  ColumnDefinitions="*,Auto">
                                <StackPanel VerticalAlignment="Center" Spacing="4">
                                    <TextBlock x:Uid="Acp_GlobalEnabledTitle"
                                               Text="启用 ACP Agent"
                                               Style="{StaticResource SettingsRowTitleTextStyle}"/>
                                    <TextBlock x:Uid="Acp_GlobalEnabledDescription"
                                               Text="关闭后，Salmon Egg 不会自动启动、复用或恢复 ACP 会话连接。"
                                               Style="{StaticResource SettingsRowDescriptionTextStyle}"/>
                                </StackPanel>

                                <ToggleSwitch Grid.Column="1"
                                              x:Name="AcpGlobalEnabledToggle"
                                              x:Uid="Acp_GlobalEnabledSwitch"
                                              AutomationProperties.AutomationId="Acp.Global.Enabled"
                                              IsOn="{x:Bind ViewModel.IsAcpEnabled, Mode=TwoWay}"
                                              OnContent="开启"
                                              OffContent="关闭"
                                              VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <!-- Profiles -->
                    <StackPanel Spacing="12">
                        <Grid ColumnDefinitions="*,Auto">
                            <TextBlock x:Uid="Acp_ProfilesTitle"
                                       Text="ACP 连接配置"
                                       Style="{StaticResource SettingsSectionTitleTextStyle}"
                                       VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1"
                                        Orientation="Horizontal"
                                        Spacing="8"
                                        VerticalAlignment="Center">
                                <Button x:Name="AcpProfilesRefreshButton"
                                        x:Uid="Acp_ProfilesRefresh"
                                        Content="刷新"
                                        AutomationProperties.AutomationId="Acp.Profiles.Refresh"
                                        Command="{x:Bind ViewModel.Profiles.RefreshCommand}"
                                        IsEnabled="{x:Bind ViewModel.Profiles.IsLoading, Mode=OneWay, Converter={StaticResource InverseBooleanConverter}}"/>
                                <Button x:Name="AcpProfilesAddButton"
                                        x:Uid="Acp_ProfilesAdd"
                                        Content="新建配置"
                                        AutomationProperties.AutomationId="Acp.Profiles.Add"
                                        Style="{StaticResource AccentButtonStyle}"
                                        Click="OnAddProfileClick"/>
                            </StackPanel>
                        </Grid>

                        <Border Style="{StaticResource SettingsSectionContainerStyle}">
                            <ListView ItemsSource="{x:Bind ViewModel.Profiles.ProfileItems, Mode=OneWay}"
                                      SelectedItem="{x:Bind ViewModel.Profiles.SelectedProfileItem, Mode=TwoWay}"
                                      SelectionMode="Single"
                                      MinHeight="140"
                                      Background="Transparent"
                                      ItemContainerStyle="{StaticResource AgentListItemStyle}">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="vm:AgentProfileItemViewModel">
                                        <Grid Padding="16,14"
                                              ColumnDefinitions="Auto,*,Auto"
                                              ColumnSpacing="16"
                                              RowDefinitions="Auto,Auto"
                                              RowSpacing="4">
                                            <Border Grid.RowSpan="2"
                                                    Width="36"
                                                    Height="36"
                                                    CornerRadius="8"
                                                    Background="{ThemeResource AccentFillColorDefaultBrush}"
                                                    Opacity="0.18"
                                                    VerticalAlignment="Center">
                                                <Viewbox Width="18" Height="18" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                              Glyph="{x:Bind TransportGlyph}"
                                                              Foreground="{ThemeResource AccentBrush}"/>
                                                </Viewbox>
                                            </Border>

                                            <Grid Grid.Column="1"
                                                  ColumnDefinitions="*,Auto"
                                                  ColumnSpacing="10"
                                                  VerticalAlignment="Center">
                                                <TextBlock Text="{x:Bind Name}"
                                                           FontWeight="SemiBold"
                                                           TextTrimming="CharacterEllipsis"
                                                           VerticalAlignment="Center"/>

                                                <Grid Grid.Column="1"
                                                      VerticalAlignment="Center">
                                                    <Border CornerRadius="12"
                                                            Padding="8,4"
                                                            Background="{ThemeResource SystemFillColorSuccessBackgroundBrush}"
                                                            Visibility="{x:Bind IsConnected, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <TextBlock Text="{x:Bind StatusLabel, Mode=OneWay}"
                                                                   FontSize="12"
                                                                   FontWeight="Medium"
                                                                   Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                                                    </Border>

                                                    <Border CornerRadius="12"
                                                            Padding="8,4"
                                                            Background="{ThemeResource SystemFillColorNeutralBrush}"
                                                            Visibility="{x:Bind IsNotConnected, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <TextBlock Text="{x:Bind StatusLabel, Mode=OneWay}"
                                                                   FontSize="12"
                                                                   FontWeight="Medium"
                                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                    </Border>
                                                </Grid>
                                            </Grid>

                                            <TextBlock Grid.Row="1"
                                                       Grid.Column="1"
                                                       Text="{x:Bind EndpointDescription}"
                                                       FontSize="12"
                                                       Opacity="0.65"
                                                       TextTrimming="CharacterEllipsis"/>

                                            <Grid Grid.RowSpan="2"
                                                  Grid.Column="2"
                                                  ColumnDefinitions="Auto,Auto"
                                                  ColumnSpacing="10"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Right">
                                                <ToggleSwitch VerticalAlignment="Center"
                                                              OffContent=""
                                                              OnContent=""
                                                              IsOn="{x:Bind IsConnected, Mode=OneWay}"
                                                              IsEnabled="{x:Bind IsConnecting, Mode=OneWay, Converter={StaticResource InverseBooleanConverter}}"
                                                              Toggled="OnProfileConnectionToggleToggled"/>

                                                <Button Grid.Column="1"
                                                        VerticalAlignment="Center"
                                                        x:Uid="Acp_ProfileMore"
                                                        ToolTipService.ToolTip="更多">
                                                    <Button.Content>
                                                        <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE712;"/>
                                                    </Button.Content>
                                                    <Button.Flyout>
                                                        <MenuFlyout>
                                                            <MenuFlyoutItem x:Uid="Acp_ProfileEdit"
                                                                            Text="编辑"
                                                                            Tag="{x:Bind ProfileId}"
                                                                            Click="OnEditProfileMenuClick"/>
                                                            <MenuFlyoutItem x:Uid="Acp_ProfileDelete"
                                                                            Text="删除"
                                                                            Tag="{x:Bind ProfileId}"
                                                                            Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                                                            Click="OnDeleteProfileMenuClick"/>
                                                        </MenuFlyout>
                                                    </Button.Flyout>
                                                </Button>
                                            </Grid>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>
                    </StackPanel>

                    <!-- Remote directories -->
                    <StackPanel Spacing="12"
                                AutomationProperties.AutomationId="Acp.RemoteDirectories.Section">
                        <Grid ColumnDefinitions="*,Auto">
                            <TextBlock x:Uid="Acp_RemoteDirectoriesTitle"
                                       Text="远端目录"
                                       Style="{StaticResource SettingsSectionTitleTextStyle}"
                                       VerticalAlignment="Center"/>
                            <Button Grid.Column="1"
                                    x:Name="AcpRemoteDirectoriesAddButton"
                                    x:Uid="Acp_RemoteDirectoriesAdd"
                                    Content="新增目录"
                                    Command="{x:Bind ViewModel.AddRemoteDirectoryCommand}"
                                    AutomationProperties.AutomationId="Acp.RemoteDirectories.Add"/>
                        </Grid>

                        <TextBlock x:Uid="Acp_RemoteDirectoriesHint"
                                   Text="为当前 Agent 配置可用于新建远程 ACP 会话的远端工作目录。Salmon Egg 不会把本机路径发送给远端 Agent。"
                                   Style="{StaticResource SettingsRowDescriptionTextStyle}"/>

                        <Border Style="{StaticResource SettingsSectionContainerStyle}">
                            <ListView ItemsSource="{x:Bind ViewModel.RemoteDirectoryRows, Mode=OneWay}"
                                      SelectionMode="None"
                                      MinHeight="120"
                                      Background="Transparent"
                                      ItemContainerStyle="{StaticResource AgentListItemStyle}"
                                      AutomationProperties.AutomationId="Acp.RemoteDirectories.List">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="vm:AcpRemoteDirectoryRowViewModel">
                                        <Grid Style="{StaticResource SettingsRowGridStyle}"
                                              ColumnDefinitions="*,Auto"
                                              RowDefinitions="Auto,Auto"
                                              RowSpacing="8">
                                            <TextBox Grid.Row="0" Grid.Column="0"
                                                     x:Uid="Acp_RemoteDirectoriesDisplayName"
                                                     Header="显示名称"
                                                     PlaceholderText="Workspace"
                                                     Text="{x:Bind DisplayName, Mode=TwoWay}"/>
                                            <TextBox Grid.Row="1" Grid.Column="0"
                                                     x:Uid="Acp_RemoteDirectoriesRemotePath"
                                                     Header="远端目录"
                                                     PlaceholderText="/home/user/project"
                                                     Text="{x:Bind RemotePath, Mode=TwoWay}"/>
                                            <Button Grid.Row="0" Grid.RowSpan="2" Grid.Column="1"
                                                    x:Uid="Acp_RemoteDirectoriesRemove"
                                                    Command="{x:Bind RemoveCommand}"
                                                    VerticalAlignment="Center">
                                                <Button.Content>
                                                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                              Glyph="&#xE74D;"/>
                                                </Button.Content>
                                            </Button>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>
                    </StackPanel>

                    <!-- Hydration policy -->
                    <StackPanel Spacing="12">
                        <TextBlock x:Uid="Acp_HydrationCompletionTitle"
                                   Text="会话加载完成判定"
                                   Style="{StaticResource SettingsSectionTitleTextStyle}"/>

                        <Border Style="{StaticResource SettingsSectionContainerStyle}">
                            <Expander x:Uid="Acp_AdvancedExpander"
                                      Header="高级加载策略"
                                      IsExpanded="False"
                                      Background="Transparent"
                                      HorizontalAlignment="Stretch"
                                      HorizontalContentAlignment="Stretch"
                                      Padding="0">
                                <StackPanel Padding="{StaticResource SettingsRowPadding}"
                                            Spacing="12"
                                            HorizontalAlignment="Stretch">
                                    <StackPanel Spacing="4">
                                        <TextBlock x:Uid="Acp_HydrationCompletionHint"
                                                   Text="SalmonEgg 使用此设置决定远程 ACP 会话在 session/load 返回后何时可视为“已就绪”。"
                                                   Style="{StaticResource SettingsRowDescriptionTextStyle}"/>
                                        <TextBlock Text="{x:Bind ViewModel.SelectedHydrationCompletionMode, Mode=OneWay, Converter={StaticResource HydrationCompletionModeLocalizationConverter}, ConverterParameter=Description}"
                                                   Style="{StaticResource SettingsRowDescriptionTextStyle}"/>
                                    </StackPanel>

                                    <ComboBox ItemsSource="{x:Bind ViewModel.HydrationCompletionModeOptions, Mode=OneWay}"
                                              SelectedItem="{x:Bind ViewModel.SelectedHydrationCompletionMode, Mode=TwoWay}"
                                              MinWidth="{StaticResource SettingsRowControlMinWidth}"
                                              IsFocusEngagementEnabled="True"
                                              HorizontalAlignment="Stretch">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate x:DataType="vm:HydrationCompletionModeOptionViewModel">
                                                <TextBlock Text="{x:Bind Value, Converter={StaticResource HydrationCompletionModeLocalizationConverter}}"/>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                </StackPanel>
                            </Expander>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </controls:ResponsiveContentHost.Child>
        </controls:ResponsiveContentHost>
    </ScrollViewer>
</views:SettingsPageBase>
