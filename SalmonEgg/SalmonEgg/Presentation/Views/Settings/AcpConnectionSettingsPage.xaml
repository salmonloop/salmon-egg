<Page
    x:Class="SalmonEgg.Presentation.Views.Settings.AcpConnectionSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:SalmonEgg.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:SalmonEgg.Presentation.ViewModels.Settings"
    xmlns:models="using:SalmonEgg.Domain.Models"
    xmlns:converters="using:SalmonEgg.Presentation.Converters"
    mc:Ignorable="d"
    Background="Transparent"
    AllowDrop="False">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </Page.Resources>

    <ScrollViewer Padding="40,24"
                  HorizontalScrollBarVisibility="Disabled">
        <controls:ResponsiveSettingsHost MaxContentWidth="{StaticResource SettingsContentMaxWidth}">
            <controls:ResponsiveSettingsHost.Child>
                <StackPanel Spacing="32">
                <TextBlock Text="连接（ACP）" FontSize="28" FontWeight="Bold" Margin="0,0,0,12"/>

            <!-- Active connection -->
            <StackPanel Spacing="12">
                <TextBlock Text="当前连接" FontSize="18" FontWeight="SemiBold" Foreground="{ThemeResource AccentBrush}"/>

                <Border Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="16">
                        <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto">
                            <StackPanel Grid.Row="0" Grid.Column="0" Spacing="2">
                                <TextBlock Text="{x:Bind ViewModel.Chat.AgentName, Mode=OneWay}" FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind ViewModel.Chat.AgentVersion, Mode=OneWay}"
                                           Opacity="0.65"
                                           FontSize="12"/>
                            </StackPanel>
                            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                <Button Content="连接"
                                        Style="{StaticResource AccentButtonStyle}"
                                        Command="{x:Bind ViewModel.Chat.InitializeAndConnectCommand}"
                                        IsEnabled="{x:Bind ViewModel.Chat.IsConnecting, Mode=OneWay, Converter={StaticResource InverseBooleanConverter}}"/>
                                <Button Content="断开"
                                        Command="{x:Bind ViewModel.Chat.DisconnectCommand}"
                                        IsEnabled="{x:Bind ViewModel.Chat.IsConnected, Mode=OneWay}"/>
                            </StackPanel>
                        </Grid>

                        <StackPanel Spacing="8">
                            <TextBlock Text="传输协议" FontWeight="Medium"/>
                            <ComboBox ItemsSource="{x:Bind ViewModel.TransportOptions, Mode=OneWay}"
                                      SelectedItem="{x:Bind ViewModel.SelectedTransport, Mode=TwoWay}"
                                      DisplayMemberPath="Name"
                                      MinWidth="240"/>
                        </StackPanel>

                        <StackPanel Spacing="12" Visibility="{x:Bind ViewModel.Chat.TransportConfig.IsStdio, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBox Header="执行命令"
                                     PlaceholderText="例如：python / node / agent.exe"
                                     Text="{x:Bind ViewModel.Chat.TransportConfig.StdioCommand, Mode=TwoWay}"/>
                            <TextBox Header="参数"
                                     PlaceholderText="例如：server.py --port 8080"
                                     Text="{x:Bind ViewModel.Chat.TransportConfig.StdioArgs, Mode=TwoWay}"/>
                        </StackPanel>

                        <StackPanel Spacing="12" Visibility="{x:Bind ViewModel.Chat.TransportConfig.IsStdio, Mode=OneWay, Converter={StaticResource InverseBooleanConverter}}">
                            <TextBox Header="服务器地址"
                                     PlaceholderText="wss://example.com 或 http://localhost:8080/sse"
                                     Text="{x:Bind ViewModel.Chat.TransportConfig.RemoteUrl, Mode=TwoWay}"/>
                        </StackPanel>

                        <InfoBar IsOpen="{x:Bind ViewModel.Chat.HasConnectionError, Mode=OneWay}"
                                 Severity="Error"
                                 Title="连接失败"
                                 Message="{x:Bind ViewModel.Chat.ConnectionErrorMessage, Mode=OneWay}"
                                 IsClosable="True"/>

                        <InfoBar IsOpen="{x:Bind ViewModel.Chat.IsConnected, Mode=OneWay}"
                                 Severity="Success"
                                 Title="已连接"
                                 Message="已与 ACP 服务端建立连接"
                                 IsClosable="True"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Profiles -->
            <StackPanel Spacing="12">
                <Grid>
                    <TextBlock Text="预设服务器" FontSize="18" FontWeight="SemiBold" Foreground="{ThemeResource AccentBrush}" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                        <Button Content="刷新"
                                Command="{x:Bind ViewModel.Profiles.RefreshCommand}"
                                IsEnabled="{x:Bind ViewModel.Profiles.IsLoading, Mode=OneWay, Converter={StaticResource InverseBooleanConverter}}"/>
                        <Button Content="新建"
                                Style="{StaticResource AccentButtonStyle}"
                                Click="OnAddProfileClick"/>
                    </StackPanel>
                </Grid>

                <ListView ItemsSource="{x:Bind ViewModel.Profiles.Profiles, Mode=OneWay}"
                          SelectedItem="{x:Bind ViewModel.Profiles.SelectedProfile, Mode=TwoWay}"
                          SelectionMode="Single"
                          Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
                          BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                          BorderThickness="1"
                          CornerRadius="8"
                          MinHeight="140">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:ServerConfiguration">
                            <Grid Padding="12,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="2">
                                    <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{x:Bind ServerUrl}" FontSize="12" Opacity="0.65"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                                    <Button Content="&#xE70F;" FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                            Style="{StaticResource SubtleButtonStyle}"
                                            Tag="{x:Bind}"
                                            Click="OnEditProfileClick"
                                            ToolTipService.ToolTip="编辑"/>
                                    <Button Content="&#xE74D;" FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                            Style="{StaticResource SubtleButtonStyle}"
                                            Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                            Tag="{x:Bind}"
                                            Click="OnDeleteProfileClick"
                                            ToolTipService.ToolTip="删除"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
                </StackPanel>
            </controls:ResponsiveSettingsHost.Child>
        </controls:ResponsiveSettingsHost>
    </ScrollViewer>
</Page>
