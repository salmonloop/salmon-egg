<views:SettingsPageBase
    x:Class="SalmonEgg.Presentation.Views.Settings.ShortcutsSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:SalmonEgg.Controls"
    xmlns:views="using:SalmonEgg.Presentation.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:SalmonEgg.Presentation.ViewModels.Settings"
    mc:Ignorable="d"
    Background="Transparent"
    AllowDrop="False">

    <ScrollViewer Padding="0"
                  HorizontalScrollBarVisibility="Disabled">
            <controls:ResponsiveSettingsHost MaxContentWidth="{StaticResource SettingsContentMaxWidth}">
            <controls:ResponsiveSettingsHost.Child>
                <StackPanel Spacing="24">
            <InfoBar x:Uid="Shortcuts_ConflictInfo"
                     IsOpen="{x:Bind ViewModel.HasConflicts, Mode=OneWay}"
                     Title="快捷键冲突"
                     Message="{x:Bind ViewModel.ConflictMessage, Mode=OneWay}"
                     Severity="Warning"
                     IsClosable="False"/>

            <InfoBar x:Uid="Shortcuts_InvalidInfo"
                     IsOpen="{x:Bind ViewModel.HasInvalid, Mode=OneWay}"
                     Title="快捷键格式无效"
                     Message="请输入如 Ctrl+K、Ctrl+Shift+N 的格式。"
                     Severity="Error"
                     IsClosable="False"/>

            <StackPanel Spacing="12">
                <TextBlock x:Uid="Shortcuts_CustomTitle" Text="自定义" FontSize="18" FontWeight="SemiBold" Foreground="{ThemeResource AccentBrush}"/>
                <Border Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="12">
                        <ListView ItemsSource="{x:Bind ViewModel.Shortcuts, Mode=OneWay}"
                                  SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="vm:ShortcutEntryViewModel">
                                    <Grid Padding="0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="220"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" Spacing="2">
                                            <TextBlock Text="{x:Bind Name, Mode=OneWay}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{x:Bind DefaultGesture, Mode=OneWay}" FontSize="12" Opacity="0.65"/>
                                        </StackPanel>

                                        <TextBox Grid.Column="1"
                                                 Text="{x:Bind Gesture, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                 x:Uid="Shortcuts_GestureInput"
                                                 PlaceholderText="例如：Ctrl+K"
                                                 HorizontalAlignment="Stretch" />

                                        <Button Grid.Column="2"
                                                x:Uid="Shortcuts_RestoreSingle"
                                                Content="恢复"
                                                Margin="12,0,0,0"
                                                Command="{x:Bind RestoreDefaultCommand}" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Uid="Shortcuts_RestoreAll"
                                    Content="全部恢复默认"
                                    Command="{x:Bind ViewModel.RestoreDefaultsCommand}" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
                </StackPanel>
            </controls:ResponsiveSettingsHost.Child>
        </controls:ResponsiveSettingsHost>
    </ScrollViewer>
</views:SettingsPageBase>
