<views:SettingsPageBase
    x:Class="SalmonEgg.Presentation.Views.Settings.AcpSetupWizardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:SalmonEgg.Controls"
    xmlns:views="using:SalmonEgg.Presentation.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:SalmonEgg.Presentation.Converters"
    xmlns:acpvm="using:SalmonEgg.Presentation.ViewModels.Settings.AcpSetup"
    mc:Ignorable="d"
    Background="Transparent"
    NavigationCacheMode="Disabled">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </Page.Resources>

    <Grid>
        <ScrollViewer Padding="0" HorizontalScrollBarVisibility="Disabled">
            <controls:ResponsiveContentHost MaxContentWidth="{StaticResource SettingsContentMaxWidth}">
                <controls:ResponsiveContentHost.Child>
                    <StackPanel x:Name="WizardContentRoot" Spacing="16">

                        <StackPanel Spacing="8">
                            <TextBlock x:Uid="AcpSetup_PageTitle"
                                       Text="ACP Setup Wizard"
                                       Style="{StaticResource SettingsPageTitleTextStyle}"/>
                            <TextBlock x:Uid="AcpSetup_PageSummary"
                                       Style="{StaticResource SettingsPageSummaryTextStyle}"/>
                            <TextBlock x:Name="AcpSetupStepPosition"
                                       Text="{x:Bind ViewModel.StepPositionText, Mode=OneWay}"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>

                        <!-- Operation failure surface, shared by every command. -->
                        <InfoBar x:Name="AcpSetupErrorBar"
                                 IsOpen="{x:Bind ViewModel.HasErrorMessage, Mode=OneWay}"
                                 Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                                 Severity="Error"
                                 IsClosable="True"/>

                        <!-- ── Step 1: agent selection ─────────────────────── -->
                        <StackPanel x:Name="AgentSelectionPanel"
                                    Visibility="{x:Bind ViewModel.IsOnAgentSelection, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                    Spacing="12">
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock x:Uid="AcpSetup_AgentsTitle"
                                           Style="{StaticResource SettingsSectionTitleTextStyle}"
                                           VerticalAlignment="Center"/>
                                <Button Grid.Column="1"
                                        x:Name="AcpSetupDetectAgentsButton"
                                        x:Uid="AcpSetup_DetectAgents"
                                        AutomationProperties.AutomationId="AcpSetup.Agents.Detect"
                                        Command="{x:Bind ViewModel.DetectAgentsCommand}"/>
                            </Grid>

                            <ListView x:Name="AcpSetupAgentsList"
                                      ItemsSource="{x:Bind ViewModel.Agents}"
                                      SelectedItem="{x:Bind ViewModel.SelectedAgent, Mode=TwoWay}"
                                      SelectionMode="Single">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="acpvm:AcpSetupAgentRowViewModel">
                                        <StackPanel Spacing="4" Padding="12,8">
                                            <Grid ColumnDefinitions="*,Auto">
                                                <TextBlock Text="{x:Bind DisplayName}" FontWeight="SemiBold"/>
                                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                                    <!-- Every status affordance here reflects a probe that
                                                         runs after the row is realized, so each binding is
                                                         OneWay: a row starts Undetermined, which renders as
                                                         none of these, and a OneTime binding would freeze it
                                                         that way for the wizard's lifetime. -->
                                                    <TextBlock Text="{x:Bind Version, Mode=OneWay}"
                                                              Visibility="{x:Bind HasVersion, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                              Glyph="&#xE73E;"
                                                              x:Uid="AcpSetup_StatusInstalled"
                                                              FontSize="14"
                                                              Visibility="{x:Bind IsInstalled, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                              Glyph="&#xE711;"
                                                              x:Uid="AcpSetup_StatusMissing"
                                                              FontSize="14"
                                                              Visibility="{x:Bind IsMissing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                    <ProgressRing IsActive="{x:Bind IsChecking, Mode=OneWay}" Width="16" Height="16"/>
                                                </StackPanel>
                                            </Grid>
                                            <TextBlock Text="{x:Bind Description}"
                                                      Style="{StaticResource CaptionTextBlockStyle}"
                                                      TextWrapping="Wrap"
                                                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                            <!-- Found: say where, so the user can tell which install
                                                 of several the wizard picked. -->
                                            <StackPanel Orientation="Horizontal" Spacing="6"
                                                        Visibility="{x:Bind HasResolvedPath, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <TextBlock x:Uid="AcpSetup_ResolvedPathLabel"
                                                           Style="{StaticResource CaptionTextBlockStyle}"
                                                           Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                                                <TextBlock Text="{x:Bind ResolvedPath, Mode=OneWay}"
                                                           Style="{StaticResource CaptionTextBlockStyle}"
                                                           FontFamily="Consolas"
                                                           TextTrimming="CharacterEllipsis"
                                                           IsTextSelectionEnabled="True"
                                                           Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                                            </StackPanel>

                                            <!-- Shadowed installs. Absent on an ordinary machine: the
                                                 whole block is gated on there actually being a choice,
                                                 so a rare case costs nothing in the common one. -->
                                            <StackPanel Spacing="6"
                                                        Visibility="{x:Bind HasMultipleCandidates, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <TextBlock x:Uid="AcpSetup_MultipleCandidates"
                                                           Style="{StaticResource CaptionTextBlockStyle}"
                                                           TextWrapping="Wrap"
                                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                <!-- Selection is native ComboBox state bound to the row,
                                                     so no code-behind writes it back. -->
                                                <ComboBox x:Uid="AcpSetup_CandidatePicker"
                                                          IsFocusEngagementEnabled="True"
                                                          ItemsSource="{x:Bind Candidates, Mode=OneWay}"
                                                          SelectedItem="{x:Bind SelectedCandidate, Mode=TwoWay}"
                                                          HorizontalAlignment="Stretch"
                                                          AutomationProperties.AutomationId="AcpSetup.Agents.CandidatePicker"/>
                                            </StackPanel>

                                            <!-- Not found: name the command that was looked for, then
                                                 keep the explanation and the fix one disclosure away so
                                                 a list of eight agents stays scannable. -->
                                            <StackPanel Spacing="6"
                                                        Visibility="{x:Bind IsMissing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <StackPanel Orientation="Horizontal" Spacing="8">
                                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                                        <TextBlock x:Uid="AcpSetup_ProbeTargetLabel"
                                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                        <TextBlock Text="{x:Bind ProbeCommand}"
                                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                                   FontFamily="Consolas"
                                                                   IsTextSelectionEnabled="True"
                                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                    </StackPanel>
                                                    <!-- Offered only when this machine can honour it:
                                                         CanInstallHere folds the catalog's install path
                                                         together with a probe of the toolchain that would
                                                         run it, so the button is absent rather than
                                                         failing on click. -->
                                                    <Button x:Name="InstallAgentInlineButton"
                                                            x:Uid="AcpSetup_InstallAgentRow"
                                                            Tag="{x:Bind}"
                                                            Click="OnInstallAgentRowClick"
                                                            AutomationProperties.AutomationId="AcpSetup.Agents.InstallRow"
                                                            Visibility="{x:Bind CanInstallHere, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                    <!-- Distribution the wizard never automates (a
                                                         prebuilt binary): the agent's own install page is
                                                         the right destination. -->
                                                    <HyperlinkButton NavigateUri="{x:Bind InstallDocumentation}"
                                                                     x:Uid="AcpSetup_InstallDocs"
                                                                     Visibility="{x:Bind HasAutomaticInstallPath, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}"/>
                                                    <!-- Installable in principle, but the toolchain is
                                                         absent. Points at the toolchain rather than the
                                                         agent, because the agent's page assumes a
                                                         toolchain this user does not have yet. -->
                                                    <HyperlinkButton NavigateUri="{x:Bind ToolchainDocumentation, Mode=OneWay}"
                                                                     x:Uid="AcpSetup_InstallToolchainDocs"
                                                                     AutomationProperties.AutomationId="AcpSetup.Agents.ToolchainDocs"
                                                                     Visibility="{x:Bind IsToolchainMissing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                </StackPanel>

                                                <!-- Says why there is no install button, naming the
                                                     toolchain. Withdrawing the button without this would
                                                     leave the row looking like the agent simply cannot be
                                                     installed, which is a different and wrong conclusion.

                                                     Text is composed by the row rather than carrying an
                                                     x:Uid: the sentence interpolates the toolchain name,
                                                     and an x:Uid would assign .Text from the resw and
                                                     overwrite this binding. -->
                                                <TextBlock Text="{x:Bind ToolchainMissingHint, Mode=OneWay}"
                                                           Visibility="{x:Bind IsToolchainMissing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                                           AutomationProperties.AutomationId="AcpSetup.Agents.ToolchainMissingHint"
                                                           Style="{StaticResource CaptionTextBlockStyle}"
                                                           TextWrapping="Wrap"
                                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                                                <Expander x:Name="AgentProbeDiagnosticsExpander"
                                                          IsExpanded="False"
                                                          HorizontalAlignment="Stretch"
                                                          HorizontalContentAlignment="Stretch">
                                                    <Expander.Header>
                                                        <TextBlock x:Uid="AcpSetup_WhyNotFound"/>
                                                    </Expander.Header>
                                                    <StackPanel Spacing="8">
                                                        <TextBlock x:Uid="AcpSetup_PathHint"
                                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                                   TextWrapping="Wrap"
                                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                        <TextBlock Text="{x:Bind ProbeDetail, Mode=OneWay}"
                                                                   Visibility="{x:Bind HasProbeDetail, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                                   FontFamily="Consolas"
                                                                   TextWrapping="Wrap"
                                                                   IsTextSelectionEnabled="True"
                                                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                                                        <TextBox x:Uid="AcpSetup_CustomCommand"
                                                                 Text="{x:Bind CustomCommand, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                 AutomationProperties.AutomationId="AcpSetup.Agents.CustomCommand"/>
                                                        <Button x:Name="VerifyCustomCommandButton"
                                                                x:Uid="AcpSetup_VerifyCustomCommand"
                                                                Tag="{x:Bind}"
                                                                Click="OnVerifyAgentRowClick"
                                                                IsEnabled="{x:Bind HasCustomCommand, Mode=OneWay}"
                                                                AutomationProperties.AutomationId="AcpSetup.Agents.VerifyCustomCommand"
                                                                HorizontalAlignment="Left"/>
                                                    </StackPanel>
                                                </Expander>
                                            </StackPanel>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>

                        <!-- ── Step 2: components (runtime + adapter) ──────── -->
                        <StackPanel x:Name="ComponentSetupPanel"
                                    Visibility="{x:Bind ViewModel.IsOnComponentSetup, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                    Spacing="12">
                            <TextBlock x:Uid="AcpSetup_ComponentsTitle"
                                       Style="{StaticResource SettingsSectionTitleTextStyle}"/>

                            <!-- Each probe verdict gets its own surface. "Undetermined" is deliberately
                                 not styled as a failure: the wizard could not look, which is not
                                 evidence the adapter is absent. -->
                            <InfoBar x:Name="AcpSetupAdapterMissingBar"
                                     x:Uid="AcpSetup_AdapterMissingBar"
                                     IsOpen="{x:Bind ViewModel.IsAdapterMissing, Mode=OneWay}"
                                     Severity="Warning"
                                     IsClosable="False">
                                <!-- Two actions, so they go in Content: ActionButton holds a single
                                     ButtonBase and a panel there would not render as buttons. -->
                                <InfoBar.Content>
                                    <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
                                        <Button x:Name="AcpSetupInstallAdapterButton"
                                                x:Uid="AcpSetup_InstallAdapter"
                                                Style="{StaticResource AccentButtonStyle}"
                                                AutomationProperties.AutomationId="AcpSetup.Adapter.Install"
                                                Command="{x:Bind ViewModel.InstallAdapterCommand}"/>
                                        <Button x:Name="AcpSetupRedetectAdapterButton"
                                                x:Uid="AcpSetup_RedetectAdapter"
                                                AutomationProperties.AutomationId="AcpSetup.Adapter.Redetect"
                                                Command="{x:Bind ViewModel.DetectAdapterCommand}"/>
                                        <!-- The install button above is command-bound, so a missing
                                             toolchain already disables it. A disabled button explains
                                             nothing on its own, so the way forward is offered beside it:
                                             the toolchain's own documentation, named in the text below. -->
                                        <HyperlinkButton x:Name="AcpSetupAdapterToolchainDocsButton"
                                                         x:Uid="AcpSetup_InstallToolchainDocs"
                                                         NavigateUri="{x:Bind ViewModel.AdapterToolchainDocumentation, Mode=OneWay}"
                                                         AutomationProperties.AutomationId="AcpSetup.Adapter.ToolchainDocs"
                                                         Visibility="{x:Bind ViewModel.IsAdapterToolchainMissing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    </StackPanel>
                                </InfoBar.Content>
                            </InfoBar>

                            <!-- Named separately from the warning above because it is a different
                                 problem with a different fix: the adapter is installable, but nothing on
                                 this machine can install it yet. -->
                            <InfoBar x:Name="AcpSetupAdapterToolchainMissingBar"
                                     IsOpen="{x:Bind ViewModel.IsAdapterToolchainMissing, Mode=OneWay}"
                                     Message="{x:Bind ViewModel.AdapterToolchainMissingText, Mode=OneWay}"
                                     AutomationProperties.AutomationId="AcpSetup.Adapter.ToolchainMissingBar"
                                     Severity="Warning"
                                     IsClosable="False"/>

                            <InfoBar x:Name="AcpSetupAdapterReadyBar"
                                     x:Uid="AcpSetup_AdapterReadyBar"
                                     IsOpen="{x:Bind ViewModel.IsAdapterInstalled, Mode=OneWay}"
                                     Severity="Success"
                                     IsClosable="False"/>

                            <!-- Most of the catalog ships ACP itself. Saying so turns a step with
                                 nothing to do into a step that confirms nothing needs doing. -->
                            <InfoBar x:Name="AcpSetupAdapterBuiltInBar"
                                     x:Uid="AcpSetup_AdapterBuiltInBar"
                                     IsOpen="{x:Bind ViewModel.IsAdapterBuiltIn, Mode=OneWay}"
                                     Severity="Success"
                                     IsClosable="False"/>

                            <InfoBar x:Name="AcpSetupAdapterUndeterminedBar"
                                     x:Uid="AcpSetup_AdapterUndeterminedBar"
                                     IsOpen="{x:Bind ViewModel.IsAdapterUndetermined, Mode=OneWay}"
                                     Severity="Informational"
                                     IsClosable="False">
                                <InfoBar.ActionButton>
                                    <Button x:Name="AcpSetupRedetectAdapterUndeterminedButton"
                                            x:Uid="AcpSetup_RedetectAdapter"
                                            AutomationProperties.AutomationId="AcpSetup.Adapter.RedetectUndetermined"
                                            Command="{x:Bind ViewModel.DetectAdapterCommand}"/>
                                </InfoBar.ActionButton>
                            </InfoBar>

                            <StackPanel Orientation="Horizontal"
                                        Spacing="6"
                                        Visibility="{x:Bind ViewModel.HasAdapterPackageLocation, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock x:Uid="AcpSetup_PackageLocationLabel"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                                <TextBlock Text="{x:Bind ViewModel.AdapterPackageLocation, Mode=OneWay}"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           FontFamily="Consolas"
                                           TextTrimming="CharacterEllipsis"
                                           IsTextSelectionEnabled="True"
                                           Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                            </StackPanel>

                            <!-- Why the probe reached its verdict. Platform-layer English, so it sits
                                 below the localized copy rather than replacing it. -->
                            <TextBlock x:Name="AcpSetupAdapterProbeDetail"
                                       Text="{x:Bind ViewModel.AdapterProbeDetail, Mode=OneWay}"
                                       Visibility="{x:Bind ViewModel.HasAdapterProbeDetail, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       FontFamily="Consolas"
                                       TextWrapping="Wrap"
                                       IsTextSelectionEnabled="True"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       AutomationProperties.AutomationId="AcpSetup.Adapter.ProbeDetail"/>

                            <!-- Same disclosure as the agent rows: name the launcher that was looked
                                 for, and accept a path for it. For a packaged adapter this launcher is
                                 also the saved profile's executable, so the override has to land here. -->
                            <StackPanel Spacing="6"
                                        Visibility="{x:Bind ViewModel.HasAdapterProbeCommand, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <TextBlock x:Uid="AcpSetup_ProbeTargetLabel"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{x:Bind ViewModel.AdapterProbeCommand, Mode=OneWay}"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               FontFamily="Consolas"
                                               IsTextSelectionEnabled="True"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>

                                <Expander x:Name="AcpSetupAdapterDiagnosticsExpander"
                                          IsExpanded="False"
                                          HorizontalAlignment="Stretch"
                                          HorizontalContentAlignment="Stretch">
                                    <Expander.Header>
                                        <TextBlock x:Uid="AcpSetup_WhyNotFound"/>
                                    </Expander.Header>
                                    <StackPanel Spacing="8">
                                        <TextBlock x:Uid="AcpSetup_PathHint"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   TextWrapping="Wrap"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                        <TextBox x:Uid="AcpSetup_CustomCommand"
                                                 Text="{x:Bind ViewModel.AdapterCustomCommand, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                 AutomationProperties.AutomationId="AcpSetup.Adapter.CustomCommand"/>
                                        <Button x:Name="AcpSetupVerifyAdapterCommandButton"
                                                x:Uid="AcpSetup_VerifyCustomCommand"
                                                Command="{x:Bind ViewModel.DetectAdapterCommand}"
                                                IsEnabled="{x:Bind ViewModel.HasAdapterCustomCommand, Mode=OneWay}"
                                                AutomationProperties.AutomationId="AcpSetup.Adapter.VerifyCustomCommand"
                                                HorizontalAlignment="Left"/>
                                    </StackPanel>
                                </Expander>
                            </StackPanel>

                            <ComboBox x:Name="AcpSetupAdaptersPicker"
                                      x:Uid="AcpSetup_AdapterPicker"
                                      IsFocusEngagementEnabled="True"
                                      ItemsSource="{x:Bind ViewModel.Adapters}"
                                      SelectedItem="{x:Bind ViewModel.SelectedAdapter, Mode=TwoWay}"
                                      DisplayMemberPath="Component.DisplayName"
                                      MinWidth="280"
                                      HorizontalAlignment="Left"/>
                        </StackPanel>

                        <!-- ── Step 3: launch parameters ───────────────────── -->
                        <StackPanel x:Name="ParametersPanel"
                                    Visibility="{x:Bind ViewModel.IsOnParameters, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                    Spacing="12">
                            <TextBlock x:Uid="AcpSetup_ParametersTitle"
                                       Style="{StaticResource SettingsSectionTitleTextStyle}"/>
                            <TextBlock x:Uid="AcpSetup_CommandPreviewLabel"
                                       Style="{StaticResource BodyStrongTextBlockStyle}"/>
                            <TextBlock x:Name="AcpSetupCommandPreview"
                                       Text="{x:Bind ViewModel.LaunchCommandPreview, Mode=OneWay}"
                                       FontFamily="Consolas"
                                       TextWrapping="Wrap"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <ListView x:Name="AcpSetupParametersList"
                                      ItemsSource="{x:Bind ViewModel.Parameters}"
                                      SelectionMode="None">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="acpvm:AcpSetupParameterRowViewModel">
                                        <StackPanel Spacing="4" Padding="0,8">
                                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                                                <TextBlock Text="{x:Bind DisplayName}" VerticalAlignment="Center" MinWidth="160"/>
                                                <ComboBox Grid.Column="1"
                                                          IsFocusEngagementEnabled="True"
                                                          ItemsSource="{x:Bind AllowedValues}"
                                                          SelectedItem="{x:Bind Value, Mode=TwoWay}"
                                                          Visibility="{x:Bind HasAllowedValues, Converter={StaticResource BoolToVisibilityConverter}}"
                                                          HorizontalAlignment="Stretch"/>
                                                <TextBox Grid.Column="1"
                                                         Text="{x:Bind Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                         PlaceholderText="{x:Bind Example}"
                                                         Visibility="{x:Bind HasAllowedValues, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}"/>
                                            </Grid>
                                            <TextBlock Text="{x:Bind Description}"
                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                            <!-- Written by validation after the user tries to advance, so
                                                 OneWay. Bound OneTime this row stays blank and "Next"
                                                 silently does nothing, with no statement of what to fix. -->
                                            <TextBlock Text="{x:Bind ValidationMessage, Mode=OneWay}"
                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                       TextWrapping="Wrap"
                                                       Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                                       Visibility="{x:Bind HasValidationMessage, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>

                        <!-- ── Step 4: test ────────────────────────────────── -->
                        <StackPanel x:Name="TestPanel"
                                    Visibility="{x:Bind ViewModel.IsOnTest, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                    Spacing="12">
                            <TextBlock x:Uid="AcpSetup_TestTitle"
                                       Style="{StaticResource SettingsSectionTitleTextStyle}"/>
                            <InfoBar x:Name="AcpSetupTestSuccessBar"
                                     x:Uid="AcpSetup_TestSuccessBar"
                                     IsOpen="{x:Bind ViewModel.IsTestSuccessful, Mode=OneWay}"
                                     Severity="Success"
                                     IsClosable="False"/>
                            <!-- Bound to IsTestFailed, not HasTestResult: a passing test also
                                 produces a result, and a success bar plus an error bar both
                                 open is the bug this distinction exists to prevent. -->
                            <InfoBar x:Name="AcpSetupTestFailureBar"
                                     IsOpen="{x:Bind ViewModel.IsTestFailed, Mode=OneWay}"
                                     Severity="Error"
                                     IsClosable="False"
                                     Title="{x:Bind ViewModel.TestFailureStageText, Mode=OneWay}"
                                     Message="{x:Bind ViewModel.TestRemediationText, Mode=OneWay}"/>
                            <!-- Raw stderr excerpt / protocol error from the failed handshake.
                                 Developer-facing English, so selectable for copying into an
                                 issue; hidden entirely on success. -->
                            <TextBlock Text="{x:Bind ViewModel.TestErrorDetail, Mode=OneWay}"
                                       Visibility="{x:Bind ViewModel.HasTestErrorDetail, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                       FontFamily="Consolas"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       TextWrapping="Wrap"
                                       TextTrimming="CharacterEllipsis"
                                       MaxLines="6"
                                       IsTextSelectionEnabled="True"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                            <Button x:Name="AcpSetupTestRunButton"
                                    x:Uid="AcpSetup_RunTest"
                                    Command="{x:Bind ViewModel.TestCommand}"/>
                        </StackPanel>

                        <!-- ── Step 5: save ────────────────────────────────── -->
                        <StackPanel x:Name="SavePanel"
                                    Visibility="{x:Bind ViewModel.IsOnSave, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                    Spacing="12">
                            <TextBlock x:Uid="AcpSetup_SaveTitle"
                                       Style="{StaticResource SettingsSectionTitleTextStyle}"/>
                            <TextBox x:Name="AcpSetupProfileNameBox"
                                     x:Uid="AcpSetup_ProfileName"
                                     Text="{x:Bind ViewModel.ProfileName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     MinWidth="320"
                                     HorizontalAlignment="Left"/>
                            <Button x:Name="AcpSetupSaveButton"
                                    x:Uid="AcpSetup_Save"
                                    Style="{StaticResource AccentButtonStyle}"
                                    Command="{x:Bind ViewModel.SaveCommand}"
                                    AutomationProperties.AutomationId="AcpSetup.Save"/>
                            <InfoBar x:Name="AcpSetupSavedBar"
                                     x:Uid="AcpSetup_SavedBar"
                                     IsOpen="{x:Bind ViewModel.HasSavedProfile, Mode=OneWay}"
                                     Severity="Success"
                                     IsClosable="False"/>
                        </StackPanel>

                        <!-- Installer progress, shared by every install entry point.

                             Sits at this level rather than inside a step panel because installs start
                             from two places: the agent row's inline button on step 1, and the adapter
                             button on step 2. Nested inside step 2, a row install on step 1 showed only
                             the shared spinner — the output was being produced into a subtree that step
                             was not rendering.

                             The latest line stays visible while it runs; the full log is one disclosure
                             away because it only matters on failure. HasInstallOutput keeps the whole
                             panel absent until an install has actually produced something. -->
                        <StackPanel Spacing="8"
                                    Visibility="{x:Bind ViewModel.HasInstallOutput, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock x:Name="AcpSetupInstallOutputLatest"
                                       Text="{x:Bind ViewModel.LatestInstallOutputLine, Mode=OneWay}"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       FontFamily="Consolas"
                                       TextWrapping="NoWrap"
                                       TextTrimming="CharacterEllipsis"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       AutomationProperties.AutomationId="AcpSetup.Install.LatestLine"/>
                            <Expander x:Name="AcpSetupInstallOutputExpander"
                                      IsExpanded="False"
                                      HorizontalAlignment="Stretch"
                                      HorizontalContentAlignment="Stretch">
                                <Expander.Header>
                                    <TextBlock x:Uid="AcpSetup_InstallOutputToggle"/>
                                </Expander.Header>
                                <ListView x:Name="AcpSetupInstallOutputList"
                                          ItemsSource="{x:Bind ViewModel.InstallOutput}"
                                          SelectionMode="None"
                                          MaxHeight="200"
                                          AutomationProperties.AutomationId="AcpSetup.Install.Output">
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="x:String">
                                            <TextBlock Text="{x:Bind}"
                                                       Style="{StaticResource CaptionTextBlockStyle}"
                                                       FontFamily="Consolas"
                                                       IsTextSelectionEnabled="True"
                                                       TextWrapping="Wrap"/>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </Expander>
                        </StackPanel>

                        <!-- Busy feedback shared by every long operation, with the way out beside it.

                             Cancel sits here rather than next to each install button because the operation
                             it stops is shared: whichever entry point started it, this is where the user is
                             already looking. It is enabled only while something runs, which is the same
                             condition that reveals this panel. -->
                        <StackPanel Orientation="Horizontal" Spacing="8"
                                    Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                            <ProgressRing IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}"
                                          Width="16"
                                          Height="16"/>
                            <TextBlock x:Uid="AcpSetup_Working"
                                       VerticalAlignment="Center"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <Button x:Name="AcpSetupCancelOperationButton"
                                    x:Uid="AcpSetup_CancelOperation"
                                    Command="{x:Bind ViewModel.CancelOperationCommand}"
                                    AutomationProperties.AutomationId="AcpSetup.CancelOperation"/>
                        </StackPanel>

                        <!-- ── Wizard navigation bar ───────────────────────── -->
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8" Margin="0,8,0,24">
                            <Button x:Name="AcpSetupBackStepButton"
                                    x:Uid="AcpSetup_Back"
                                    Command="{x:Bind ViewModel.GoBackCommand}"/>
                            <Button x:Name="AcpSetupNextStepButton"
                                    Grid.Column="2"
                                    x:Uid="AcpSetup_Next"
                                    Style="{StaticResource AccentButtonStyle}"
                                    Command="{x:Bind ViewModel.GoNextCommand}"/>
                        </Grid>
                    </StackPanel>
                </controls:ResponsiveContentHost.Child>
            </controls:ResponsiveContentHost>
        </ScrollViewer>
    </Grid>
</views:SettingsPageBase>
