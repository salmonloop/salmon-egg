<views:SettingsPageBase
    x:Class="SalmonEgg.Presentation.Views.Settings.DataStorageSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:SalmonEgg.Controls"
    xmlns:views="using:SalmonEgg.Presentation.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:SalmonEgg.Presentation.Converters"
    mc:Ignorable="d"
    Background="Transparent"
    AllowDrop="False">

    <Page.Resources>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </Page.Resources>

    <ScrollViewer Padding="0"
                  HorizontalScrollBarVisibility="Disabled">
            <controls:ResponsiveSettingsHost MaxContentWidth="{StaticResource SettingsContentMaxWidth}">
            <controls:ResponsiveSettingsHost.Child>
                <StackPanel Spacing="28">
            <StackPanel Spacing="12">
                <TextBlock x:Uid="DataStorage_PrivacyTitle" Text="隐私" FontSize="18" FontWeight="SemiBold" Foreground="{ThemeResource AccentBrush}"/>
                <Border Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="12">
                        <ToggleSwitch x:Uid="DataStorage_SaveLocalHistory"
                                      Header="保存本地历史"
                                      IsOn="{x:Bind ViewModel.Preferences.SaveLocalHistory, Mode=TwoWay}"
                                      OnContent="开启"
                                      OffContent="关闭" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock x:Uid="DataStorage_CacheTitle" Text="缓存" FontSize="18" FontWeight="SemiBold" Foreground="{ThemeResource AccentBrush}"/>
                <Border Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="12">
                        <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                            <TextBlock x:Uid="DataStorage_CacheRetentionLabel" Text="缓存保留天数" VerticalAlignment="Center" />
                            <NumberBox Value="{x:Bind ViewModel.Preferences.CacheRetentionDays, Mode=TwoWay}"
                                       Minimum="1"
                                       Maximum="60"
                                       SmallChange="1"
                                       SpinButtonPlacementMode="Inline"
                                       Width="140" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Uid="DataStorage_ClearCache"
                                    Content="清理缓存"
                                    Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                    Click="OnClearCacheClick" />
                            <Button x:Uid="DataStorage_OpenCacheFolder"
                                    Content="打开缓存目录"
                                    Command="{x:Bind ViewModel.OpenCacheFolderCommand}" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock x:Uid="DataStorage_ExportTitle" Text="导出" FontSize="18" FontWeight="SemiBold" Foreground="{ThemeResource AccentBrush}"/>
                <Border Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="12">
                        <TextBlock x:Uid="DataStorage_ExportDescription" Text="导出会话（当前会话/当前消息列表）与诊断包到 exports 目录" Opacity="0.7" TextWrapping="Wrap"/>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Uid="DataStorage_ExportMarkdown" Content="导出会话（Markdown）" Command="{x:Bind ViewModel.ExportCurrentSessionMarkdownCommand}" />
                            <Button x:Uid="DataStorage_ExportJson" Content="导出会话（JSON）" Command="{x:Bind ViewModel.ExportCurrentSessionJsonCommand}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Uid="DataStorage_ExportDiagnostics" Content="导出诊断包（ZIP）" Command="{x:Bind ViewModel.CreateDiagnosticsBundleCommand}" />
                            <Button x:Uid="DataStorage_OpenExports" Content="打开导出目录" Command="{x:Bind ViewModel.OpenExportsFolderCommand}" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock x:Uid="DataStorage_ResetTitle" Text="重置" FontSize="18" FontWeight="SemiBold" Foreground="{ThemeResource AccentBrush}"/>
                <Border Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="12">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Uid="DataStorage_ResetDefaults"
                                    Content="恢复默认设置"
                                    Click="OnResetPreferencesClick" />
                            <Button x:Uid="DataStorage_ClearAllData"
                                    Content="清空所有本地数据"
                                    Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                    Click="OnClearAllLocalDataClick" />
                        </StackPanel>
                        <TextBlock x:Uid="DataStorage_ResetWarning" Text="清空本地数据会删除配置、日志、缓存等文件。此操作不可撤销。"
                                   FontSize="12"
                                   Opacity="0.7"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </StackPanel>
                </StackPanel>
            </controls:ResponsiveSettingsHost.Child>
        </controls:ResponsiveSettingsHost>
    </ScrollViewer>
</views:SettingsPageBase>
