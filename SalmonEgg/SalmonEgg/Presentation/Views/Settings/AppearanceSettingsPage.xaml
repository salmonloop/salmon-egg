<Page
    x:Class="SalmonEgg.Presentation.Views.Settings.AppearanceSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:settings="using:SalmonEgg.Presentation.ViewModels.Settings"
    mc:Ignorable="d"
    Background="Transparent"
    AllowDrop="False">

    <ScrollViewer Padding="40,24"
                  HorizontalScrollBarVisibility="Disabled">
        <StackPanel Spacing="24"
                    MaxWidth="800"
                    HorizontalAlignment="Center">
            <TextBlock Text="外观" FontSize="28" FontWeight="Bold" Margin="0,0,0,12"/>

            <StackPanel Spacing="12">
                <TextBlock Text="界面主题" FontSize="18" FontWeight="SemiBold" Foreground="{ThemeResource AccentBrush}"/>

                <Border Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="16">
                        <TextBlock Text="选择您偏好的应用主题颜色" FontSize="14"/>
                        <ComboBox SelectedValue="{x:Bind Preferences.Theme, Mode=TwoWay}"
                                  SelectedValuePath="Tag"
                                  MinWidth="240">
                            <ComboBoxItem Content="跟随系统" Tag="System" />
                            <ComboBoxItem Content="浅色" Tag="Light" />
                            <ComboBoxItem Content="深色" Tag="Dark" />
                        </ComboBox>
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock Text="交互体验" FontSize="18" FontWeight="SemiBold" Foreground="{ThemeResource AccentBrush}"/>

                <Border Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center" Spacing="2">
                            <TextBlock Text="全局过渡动画" FontWeight="Medium"/>
                            <TextBlock Text="开启后，页面切换将使用平滑过渡效果"
                                       FontSize="12"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       TextWrapping="Wrap"/>
                        </StackPanel>
                        <ToggleSwitch Grid.Column="1"
                                      IsOn="{x:Bind Preferences.IsAnimationEnabled, Mode=TwoWay}"
                                      OnContent="已开启"
                                      OffContent="已禁用"
                                      MinWidth="0"
                                      VerticalAlignment="Center"/>
                    </Grid>
                </Border>
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock Text="背景材质" FontSize="18" FontWeight="SemiBold" Foreground="{ThemeResource AccentBrush}"/>

                <Border Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="12">
                        <TextBlock Text="选择窗口背景效果（不同系统版本会自动降级）" FontSize="14" TextWrapping="Wrap"/>
                        <ComboBox SelectedValue="{x:Bind Preferences.Backdrop, Mode=TwoWay}"
                                  SelectedValuePath="Tag"
                                  MinWidth="240">
                            <ComboBoxItem Content="自动（系统推荐）" Tag="System" />
                            <ComboBoxItem Content="Mica" Tag="Mica" />
                            <ComboBoxItem Content="Acrylic" Tag="Acrylic" />
                            <ComboBoxItem Content="纯色" Tag="Solid" />
                        </ComboBox>
                    </StackPanel>
                </Border>
            </StackPanel>

            <InfoBar Title="提示"
                     Message="主题、动画和材质设置会立即生效（部分平台可能需要重启）。"
                     IsOpen="True"
                     IsClosable="False"
                     Severity="Informational" />
        </StackPanel>
    </ScrollViewer>
</Page>
