<Page
    x:Class="SalmonEgg.Presentation.Views.Start.StartView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:SalmonEgg.Controls"
    xmlns:models="using:SalmonEgg.Presentation.Models"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <Style x:Key="QuickEntryCardButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource DividerStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="MinHeight" Value="92"/>
        </Style>
    </Page.Resources>

    <Grid RowDefinitions="*,Auto,Auto">
        <Grid Grid.Row="0"
              VerticalAlignment="Center"
              Margin="20,20,20,0">
            <TextBlock Text="Salmon Egg"
                       FontSize="56"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       AutomationProperties.Name="Salmon Egg 欢迎标题"/>
        </Grid>

        <!-- 智能推荐快速入口（占位符） -->
        <Grid Grid.Row="1"
              Margin="20,0,20,12"
              MaxWidth="{x:Bind models:UiLayout.ContentMaxWidth, Mode=OneWay}"
              HorizontalAlignment="Center"
              ColumnSpacing="12"
              ColumnDefinitions="*,*,*">
            <Button Grid.Column="0"
                    Style="{StaticResource QuickEntryCardButtonStyle}"
                    IsEnabled="False"
                    AutomationProperties.Name="智能推荐入口 1">
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <SymbolIcon Symbol="Home" />
                    <StackPanel>
                        <TextBlock Text="智能推荐 1" FontWeight="SemiBold"/>
                        <TextBlock Text="占位符" Opacity="0.7" FontSize="12"/>
                    </StackPanel>
                </StackPanel>
            </Button>

            <Button Grid.Column="1"
                    Style="{StaticResource QuickEntryCardButtonStyle}"
                    IsEnabled="False"
                    AutomationProperties.Name="智能推荐入口 2">
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <SymbolIcon Symbol="Bullets" />
                    <StackPanel>
                        <TextBlock Text="智能推荐 2" FontWeight="SemiBold"/>
                        <TextBlock Text="占位符" Opacity="0.7" FontSize="12"/>
                    </StackPanel>
                </StackPanel>
            </Button>

            <Button Grid.Column="2"
                    Style="{StaticResource QuickEntryCardButtonStyle}"
                    IsEnabled="False"
                    AutomationProperties.Name="智能推荐入口 3">
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <SymbolIcon Symbol="Document" />
                    <StackPanel>
                        <TextBlock Text="智能推荐 3" FontWeight="SemiBold"/>
                        <TextBlock Text="占位符" Opacity="0.7" FontSize="12"/>
                    </StackPanel>
                </StackPanel>
            </Button>
        </Grid>

        <!-- 底部输入区（复用对话页组件） -->
        <controls:ChatInputArea Grid.Row="2"
                                PromptSubmitted="OnPromptSubmitted"/>
    </Grid>
</Page>
