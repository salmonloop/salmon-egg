<UserControl
    x:Class="SalmonEgg.Controls.ChatSkeleton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:SalmonEgg.Controls"
    xmlns:models="using:SalmonEgg.Presentation.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <UserControl.Resources>
        <Style x:Key="IncomingSkeletonItemStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="12,12,12,0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{ThemeResource ControlStrokeColorDefaultBrush}"/>
            <Setter Property="Background" Value="{ThemeResource ControlFillColorSecondaryBrush}"/>
            <Setter Property="Opacity" Value="0.4"/>
        </Style>

        <Style x:Key="OutgoingSkeletonItemStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="12,12,0,12"/>
            <Setter Property="Background" Value="{ThemeResource AccentFillColorDefaultBrush}"/>
            <Setter Property="Opacity" Value="0.22"/>
        </Style>

        <SolidColorBrush x:Key="SkeletonHighlightBrush"
                         Color="{ThemeResource SystemAltHighColor}"
                         Opacity="0.18" />
    </UserControl.Resources>

    <controls:ResponsiveContentHost MaxContentWidth="{x:Bind models:UiLayout.ContentMaxWidth, Mode=OneTime}">
        <controls:ResponsiveContentHost.Child>
            <!-- Compact default matches ChatView MessagesList short-height inset (12).
                 Comfortable restores prior 20px padding and roomier bubble gaps at
                 MinWindowHeight=760. Theme via ThemeResource only; no Storyboard. -->
            <Grid x:Name="RootGrid" Padding="12">
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup x:Name="SkeletonHeightStates">
                        <VisualState x:Name="SkeletonHeightCompact">
                            <VisualState.Setters>
                                <Setter Target="RootGrid.Padding" Value="12" />
                                <Setter Target="SkeletonRow1.Margin" Value="0,0,0,10" />
                                <Setter Target="SkeletonRow2.Margin" Value="0,0,0,14" />
                                <Setter Target="SkeletonRow3.Margin" Value="0,0,0,14" />
                                <Setter Target="SkeletonRow4.Margin" Value="0,0,0,10" />
                                <Setter Target="SkeletonRow5.Margin" Value="0,0,0,6" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="SkeletonHeightComfortable">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowHeight="760" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Target="RootGrid.Padding" Value="20" />
                                <Setter Target="SkeletonRow1.Margin" Value="0,0,0,16" />
                                <Setter Target="SkeletonRow2.Margin" Value="0,0,0,24" />
                                <Setter Target="SkeletonRow3.Margin" Value="0,0,0,24" />
                                <Setter Target="SkeletonRow4.Margin" Value="0,0,0,16" />
                                <Setter Target="SkeletonRow5.Margin" Value="0,0,0,8" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>

                <StackPanel VerticalAlignment="Top" HorizontalAlignment="Stretch">
                    <Grid x:Name="SkeletonRow1" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="8*" />
                            <ColumnDefinition Width="10*" />
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0">
                            <Border Style="{StaticResource IncomingSkeletonItemStyle}" Height="60"/>
                            <Border Style="{StaticResource IncomingSkeletonItemStyle}" Height="60" Background="{StaticResource SkeletonHighlightBrush}"/>
                        </Grid>
                    </Grid>

                    <Grid x:Name="SkeletonRow2" Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="11*" />
                            <ColumnDefinition Width="7*" />
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0">
                            <Border Style="{StaticResource IncomingSkeletonItemStyle}" Height="80"/>
                            <Border Style="{StaticResource IncomingSkeletonItemStyle}" Height="80" Background="{StaticResource SkeletonHighlightBrush}"/>
                        </Grid>
                    </Grid>

                    <Grid x:Name="SkeletonRow3" Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="11*" />
                            <ColumnDefinition Width="7*" />
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="1">
                            <Border Style="{StaticResource OutgoingSkeletonItemStyle}" Height="44"/>
                            <Border Style="{StaticResource OutgoingSkeletonItemStyle}" Height="44" Background="{StaticResource SkeletonHighlightBrush}"/>
                        </Grid>
                    </Grid>

                    <Grid x:Name="SkeletonRow4" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="9*" />
                            <ColumnDefinition Width="9*" />
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0">
                            <Border Style="{StaticResource IncomingSkeletonItemStyle}" Height="120"/>
                            <Border Style="{StaticResource IncomingSkeletonItemStyle}" Height="120" Background="{StaticResource SkeletonHighlightBrush}"/>
                        </Grid>
                    </Grid>

                    <Grid x:Name="SkeletonRow5" Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="10*" />
                            <ColumnDefinition Width="8*" />
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="1">
                            <Border Style="{StaticResource OutgoingSkeletonItemStyle}" Height="60"/>
                            <Border Style="{StaticResource OutgoingSkeletonItemStyle}" Height="60" Background="{StaticResource SkeletonHighlightBrush}"/>
                        </Grid>
                    </Grid>
                </StackPanel>
            </Grid>
        </controls:ResponsiveContentHost.Child>
    </controls:ResponsiveContentHost>
</UserControl>
