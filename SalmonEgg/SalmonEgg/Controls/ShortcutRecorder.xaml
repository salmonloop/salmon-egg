<UserControl
    x:Class="SalmonEgg.Controls.ShortcutRecorder"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Button x:Name="RecorderButton"
            Click="OnRecorderButtonClick"
            PreviewKeyDown="OnRecorderButtonPreviewKeyDown"
            KeyUp="OnRecorderButtonKeyUp"
            LostFocus="OnRecorderButtonLostFocus"
            HorizontalAlignment="Stretch"
            HorizontalContentAlignment="Left"
            AutomationProperties.AutomationId="{x:Bind RecorderAutomationId, Mode=OneWay}"
            AutomationProperties.Name="{x:Bind AutomationName, Mode=OneWay}">
        <TextBlock Text="{x:Bind DisplayText, Mode=OneWay}"
                   TextTrimming="CharacterEllipsis" />
    </Button>
</UserControl>
