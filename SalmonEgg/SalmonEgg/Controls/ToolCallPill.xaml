<UserControl
    x:Class="SalmonEgg.Controls.ToolCallPill"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:SalmonEgg.Presentation.Converters"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <Style x:Key="FlyoutCodeBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource ControlFillColorDefaultBrush}" />
            <Setter Property="Padding" Value="12" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="BorderBrush" Value="{ThemeResource ControlStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
    </UserControl.Resources>

    <Button x:Name="RootButton"
            AutomationProperties.AutomationId="ToolCallPill.RootButton"
            Padding="0"
            Background="Transparent"
            BorderThickness="0"
            HorizontalAlignment="Left"
            AutomationProperties.Name="{x:Bind AutomationName, Mode=OneWay}">
        <Button.Flyout>
            <Flyout x:Name="DetailsFlyout">
                <Flyout.FlyoutPresenterStyle>
                    <Style TargetType="FlyoutPresenter">
                        <Setter Property="Background" Value="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}" />
                        <Setter Property="CornerRadius" Value="8" />
                        <Setter Property="BorderBrush" Value="{ThemeResource SurfaceStrokeColorFlyoutBrush}" />
                        <Setter Property="Padding" Value="12" />
                        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
                        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
                    </Style>
                </Flyout.FlyoutPresenterStyle>
                <ScrollViewer MaxHeight="400" MaxWidth="450">
                    <StackPanel Spacing="12">
                        <TextBlock AutomationProperties.AutomationId="ToolCallPill.PayloadHeader"
                                   Text="{x:Bind PayloadHeaderText, Mode=OneWay}"
                                   FontWeight="Bold"
                                   FontSize="14" />
                        <Border Style="{StaticResource FlyoutCodeBorderStyle}">
                            <TextBlock x:Name="PayloadTextBlock"
                                       AutomationProperties.AutomationId="ToolCallPill.PayloadText"
                                       Text="{x:Bind RawPayload, Mode=OneWay}"
                                       FontFamily="Consolas, 'Courier New', Monospace"
                                       FontSize="11"
                                       LineHeight="16"
                                       TextWrapping="Wrap"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Flyout>
        </Button.Flyout>

        <Grid x:Name="MainContainer"
              Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
              BorderThickness="1"
              CornerRadius="8"
              Padding="4,4,12,4">
            
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Target="HoverOverlay.Opacity" Value="0" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Target="HoverOverlay.Opacity" Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

            <!-- Hover Overlay -->
            <Border x:Name="HoverOverlay"
                    Background="{ThemeResource ControlFillColorSecondaryBrush}"
                    CornerRadius="8"
                    Opacity="0">
                <Border.Transitions>
                    <TransitionCollection>
                        <EntranceThemeTransition />
                    </TransitionCollection>
                </Border.Transitions>
            </Border>

            <StackPanel Orientation="Horizontal" Spacing="0">
                <!-- Status Icon Area -->
                <Grid Width="28">
                    <ProgressRing IsActive="{x:Bind IsInProgress, Mode=OneWay}" 
                                  Width="14" Height="14" 
                                  Foreground="{ThemeResource SystemAccentColorLight2}"
                                  Visibility="{x:Bind IsInProgress, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                    
                    <FontIcon Glyph="&#xE10B;" 
                              FontSize="12" 
                              Foreground="{ThemeResource SystemFillColorSuccessBrush}"
                              Visibility="{x:Bind IsCompleted, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                    
                    <FontIcon Glyph="&#xE10A;" 
                              FontSize="12" 
                              Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                              Visibility="{x:Bind IsFailed, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                </Grid>

                <!-- Text Area -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{x:Bind DisplayToolName, Mode=OneWay}"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                               VerticalAlignment="Center" />
                    
                    <TextBlock Text="{x:Bind DisplaySummary, Mode=OneWay}"
                               FontSize="11"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Margin="8,0,0,0"
                               VerticalAlignment="Bottom"
                               Opacity="0.8"
                               TextTrimming="CharacterEllipsis"
                               MaxLines="1"
                               MaxWidth="200" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Button>
</UserControl>
