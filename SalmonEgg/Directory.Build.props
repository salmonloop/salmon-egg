<Project>
  <PropertyGroup>
    <!-- Ensure SDK resolution uses the intended Windows SDK version for WinUI builds. -->
    <WindowsSdkVersion Condition="'$(WindowsSdkVersion)' == ''">10.0.22621.0</WindowsSdkVersion>
  </PropertyGroup>
</Project>
